# SlideBlocks 配置文件
# 第一次使用：填写下面两个路径，然后运行 setup_paths.py

# 素材文件夹路径（PPT 素材所在位置，可以是网络共享盘）
# 例：D:/我的素材  或  //server/shared/素材
materials_dir: ""

# 组装结果输出目录（建议填本地路径）
# 例：D:/PPT输出
output_dir: ""

# 以下不需要修改
db_path: "slide_vault.db"
exclude_dirs:
  - "预览图"
  - "年度素材"
